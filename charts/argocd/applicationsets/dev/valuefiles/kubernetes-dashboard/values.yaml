metrics-server:
  enabled: true

kong:
  enabled: false

api:
  containers:
    args:
      - --insecure-bind-address=0.0.0.0
      - --insecure-port=8000
  
web:
  containers:
    env:
      - name: PROTOCOL
        value: "http"
