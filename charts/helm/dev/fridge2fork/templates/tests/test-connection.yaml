apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "fridge2fork.fullname" . }}-test-connection"
  labels:
    {{- include "fridge2fork.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "fridge2fork.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
