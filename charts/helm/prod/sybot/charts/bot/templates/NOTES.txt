sybot bot deployed.

  Webhook endpoint: POST /webhook/kakao
  Health endpoint:  GET  /healthz

To port-forward locally:
  kubectl --namespace {{ .Release.Namespace }} port-forward svc/{{ include "bot.fullname" . }} 8080:{{ .Values.service.port }}

{{- if .Values.externalSecret.enabled }}

GCP Secret Manager 연동:
  ExternalSecret → ClusterSecretStore: {{ .Values.externalSecret.clusterSecretStore }}
  GCP Secret: {{ .Values.externalSecret.gcpSecretName }}
  K8s Secret:  {{ .Values.externalSecret.targetSecretName }}
{{- end }}
