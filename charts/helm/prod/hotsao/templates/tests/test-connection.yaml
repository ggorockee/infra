apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "hotsao.fullname" . }}-test-connection"
  labels:
    {{- include "hotsao.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "hotsao.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
