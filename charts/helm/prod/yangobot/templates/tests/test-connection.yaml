apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "yangobot.fullname" . }}-test-connection"
  labels:
    {{- include "yangobot.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "yangobot.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
