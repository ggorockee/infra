{{- if .Values.reviewmaps.alertmanager.enabled }}
# ReviewMaps AlertManager SMTP Configuration
#
# ⚠️ 사전 준비: monitoring 네임스페이스에 SMTP 비밀번호 시크릿 생성 필요
# kubectl create secret generic {{ .Values.reviewmaps.alertmanager.smtpSecretName }} -n monitoring \
#   --from-literal=password=<SMTP_PASSWORD>

apiVersion: monitoring.coreos.com/v1alpha1
kind: AlertmanagerConfig
metadata:
  name: reviewmaps-smtp-config
  namespace: monitoring
  labels:
    app: reviewmaps
spec:
  route:
    receiver: 'reviewmaps-email'
    groupBy: ['alertname', 'severity']
    groupWait: 30s
    groupInterval: 5m
    repeatInterval: 12h
    routes:
      # Critical 알람은 즉시 발송
      - receiver: 'reviewmaps-email-critical'
        matchers:
          - name: severity
            value: critical
            matchType: "="
        groupWait: 10s
        repeatInterval: 1h
      # Warning 알람은 그룹화하여 발송
      - receiver: 'reviewmaps-email-warning'
        matchers:
          - name: severity
            value: warning
            matchType: "="
        groupWait: 5m
        repeatInterval: 6h
      # Info 알람은 하루에 한 번
      - receiver: 'reviewmaps-email-info'
        matchers:
          - name: severity
            value: info
            matchType: "="
        groupWait: 1h
        repeatInterval: 24h

  receivers:
    # Critical 알람 수신자
    - name: 'reviewmaps-email-critical'
      emailConfigs:
        - to: {{ .Values.reviewmaps.alertmanager.emailTo | quote }}
          from: 'ReviewMaps AlertManager <noreply@reviewmaps.com>'
          smarthost: 'smtp.gmail.com:587'
          authUsername: {{ .Values.reviewmaps.alertmanager.smtpUsername | quote }}
          authPassword:
            name: {{ .Values.reviewmaps.alertmanager.smtpSecretName }}
            key: {{ .Values.reviewmaps.alertmanager.smtpSecretKey }}
          requireTLS: true
          headers:
            - key: Subject
              value: '[CRITICAL] ReviewMaps {{`{{ .GroupLabels.alertname }}`}}'
          html: |
            <h2>🚨 Critical Alert: {{`{{ .GroupLabels.alertname }}`}}</h2>
            <p><strong>Severity:</strong> {{`{{ .GroupLabels.severity }}`}}</p>
            <p><strong>Service:</strong> {{`{{ .GroupLabels.service }}`}}</p>
            <p><strong>Summary:</strong> {{`{{ .CommonAnnotations.summary }}`}}</p>
            <p><strong>Description:</strong> {{`{{ .CommonAnnotations.description }}`}}</p>
            <hr>
            <h3>Alerts:</h3>
            {{`{{ range .Alerts }}`}}
            <div style="border: 2px solid red; padding: 10px; margin: 10px 0;">
              <p><strong>Alert:</strong> {{`{{ .Labels.alertname }}`}}</p>
              <p><strong>Status:</strong> {{`{{ .Status }}`}}</p>
              <p><strong>Starts At:</strong> {{`{{ .StartsAt }}`}}</p>
              {{`{{ if .Annotations.summary }}`}}<p><strong>Summary:</strong> {{`{{ .Annotations.summary }}`}}</p>{{`{{ end }}`}}
              {{`{{ if .Annotations.description }}`}}<p><strong>Description:</strong> {{`{{ .Annotations.description }}`}}</p>{{`{{ end }}`}}
            </div>
            {{`{{ end }}`}}

    # Warning 알람 수신자
    - name: 'reviewmaps-email-warning'
      emailConfigs:
        - to: {{ .Values.reviewmaps.alertmanager.emailTo | quote }}
          from: 'ReviewMaps AlertManager <noreply@reviewmaps.com>'
          smarthost: 'smtp.gmail.com:587'
          authUsername: {{ .Values.reviewmaps.alertmanager.smtpUsername | quote }}
          authPassword:
            name: {{ .Values.reviewmaps.alertmanager.smtpSecretName }}
            key: {{ .Values.reviewmaps.alertmanager.smtpSecretKey }}
          requireTLS: true
          headers:
            - key: Subject
              value: '[WARNING] ReviewMaps {{`{{ .GroupLabels.alertname }}`}}'
          html: |
            <h2>⚠️ Warning Alert: {{`{{ .GroupLabels.alertname }}`}}</h2>
            <p><strong>Severity:</strong> {{`{{ .GroupLabels.severity }}`}}</p>
            <p><strong>Service:</strong> {{`{{ .GroupLabels.service }}`}}</p>
            <p><strong>Summary:</strong> {{`{{ .CommonAnnotations.summary }}`}}</p>
            <hr>
            <h3>Alerts:</h3>
            {{`{{ range .Alerts }}`}}
            <div style="border: 1px solid orange; padding: 10px; margin: 10px 0;">
              <p><strong>Alert:</strong> {{`{{ .Labels.alertname }}`}}</p>
              <p><strong>Status:</strong> {{`{{ .Status }}`}}</p>
              <p><strong>Starts At:</strong> {{`{{ .StartsAt }}`}}</p>
            </div>
            {{`{{ end }}`}}

    # Info 알람 수신자
    - name: 'reviewmaps-email-info'
      emailConfigs:
        - to: {{ .Values.reviewmaps.alertmanager.emailTo | quote }}
          from: 'ReviewMaps AlertManager <noreply@reviewmaps.com>'
          smarthost: 'smtp.gmail.com:587'
          authUsername: {{ .Values.reviewmaps.alertmanager.smtpUsername | quote }}
          authPassword:
            name: {{ .Values.reviewmaps.alertmanager.smtpSecretName }}
            key: {{ .Values.reviewmaps.alertmanager.smtpSecretKey }}
          requireTLS: true
          headers:
            - key: Subject
              value: '[INFO] ReviewMaps {{`{{ .GroupLabels.alertname }}`}}'
          html: |
            <h2>ℹ️ Info Alert: {{`{{ .GroupLabels.alertname }}`}}</h2>
            <p><strong>Service:</strong> {{`{{ .GroupLabels.service }}`}}</p>
            <p><strong>Summary:</strong> {{`{{ .CommonAnnotations.summary }}`}}</p>

    # 기본 수신자 (fallback)
    - name: 'reviewmaps-email'
      emailConfigs:
        - to: {{ .Values.reviewmaps.alertmanager.emailTo | quote }}
          from: 'ReviewMaps AlertManager <noreply@reviewmaps.com>'
          smarthost: 'smtp.gmail.com:587'
          authUsername: {{ .Values.reviewmaps.alertmanager.smtpUsername | quote }}
          authPassword:
            name: {{ .Values.reviewmaps.alertmanager.smtpSecretName }}
            key: {{ .Values.reviewmaps.alertmanager.smtpSecretKey }}
          requireTLS: true
          headers:
            - key: Subject
              value: 'ReviewMaps Alert: {{`{{ .GroupLabels.alertname }}`}}'
{{- end }}
