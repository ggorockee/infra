apiVersion: v1
kind: Service
metadata:
  name: {{ include "fridge2fork.fullname" . }}
  labels:
    {{- include "fridge2fork.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "fridge2fork.selectorLabels" . | nindent 4 }}
